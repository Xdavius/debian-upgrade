# buildtest

Contenu de test local généré par `./build.sh`.

## Lancer la GUI (prioritaire)

```bash
./run-gui.sh
```

Mode debug:

```bash
./run-gui.sh --debug
```

## Lancer une démo dry-run CLI seule

```bash
./run-dry-run-demo.sh
```
